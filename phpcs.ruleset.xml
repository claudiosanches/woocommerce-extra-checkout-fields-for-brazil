<?xml version="1.0"?>
<ruleset name="WordPress Coding Standards">
	<!-- See https://github.com/squizlabs/PHP_CodeSniffer/wiki/Annotated-ruleset.xml -->
	<!-- See https://github.com/WordPress-Coding-Standards/WordPress-Coding-Standards/blob/develop/WordPress-Core/ruleset.xml -->

	<!-- Set a description for this ruleset. -->
	<description>A custom set of code standard rules to check for WordPress themes and plugins.</description>

	<config name="minimum_supported_wp_version" value="5.0" />
	<config name="testVersion" value="5.6-" />

	<!-- Include the WordPress ruleset. -->
	<rule ref="WordPress" />

	<!-- Set text domain. -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array" value="woocommerce-extra-checkout-fields-for-brazil" />
		</properties>
	</rule>

	<!-- Disable rules for PSR-4. -->
	<rule ref="WordPress.Files.FileName.InvalidClassFileName">
		<exclude-pattern>src/</exclude-pattern>
		<exclude-pattern>tests/php/</exclude-pattern>
	</rule>
	<rule ref="WordPress.Files.FileName.NotHyphenatedLowercase">
		<exclude-pattern>src/</exclude-pattern>
		<exclude-pattern>tests/php/</exclude-pattern>
	</rule>

	<!-- Disable rules for tests-->
	<rule ref="Generic.Files.EndFileNewline.NotFound">
		<exclude-pattern>tests/php/Fixture/views</exclude-pattern>
	</rule>
	<rule ref="Squiz.Commenting.FileComment.Missing">
		<exclude-pattern>tests/php/Fixture/views</exclude-pattern>
	</rule>
	<rule ref="WordPress.Security.EscapeOutput.OutputNotEscaped">
		<exclude-pattern>tests/php/Fixture/views</exclude-pattern>
	</rule>
	<rule ref="Internal.NoCodeFound">
		<exclude-pattern>tests/php/Fixture/views</exclude-pattern>
	</rule>
</ruleset>
